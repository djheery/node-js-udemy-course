// Working with the file system
const fileSystem = require('fs');
fileSystem.writeFileSync('hello.txt', 'Hello World, Written by the node File System package');

// Run this by uncommenting it then navigating to the terminal
// Then type: "node first-app.js" (no quotes)
console.log('Hello from node js');